/**
 * Type stub for the legacy vanilla-JS marketing module.
 *
 * The implementation lives in marketing-scripts.js (pre-migration code,
 * preserved verbatim). This declaration lets TypeScript dynamically import
 * it from <MarketingScripts /> without falling back to `any`.
 */

export function init(): void;
