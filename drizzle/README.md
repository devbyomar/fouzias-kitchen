# Drizzle migrations

SQL migrations generated by `pnpm db:generate` land here. Apply with `pnpm db:migrate`.

For dev, you can also use `pnpm db:push` to sync the schema directly without
generating migration files (faster iteration, but skips the audit trail).
